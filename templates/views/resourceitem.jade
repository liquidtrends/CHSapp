extends ../layouts/default
 
block content 
    .container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
        article
            p: a(href='/resource') ← back to resources
            hr
            if !data.post
                h2 Invalid Post.
            else
                header
                    h1= data.post.title
                .post
                    != data.post.content.full
